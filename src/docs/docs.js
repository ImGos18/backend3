/**
 * @typedef {import('express').Request} Req
 * @typedef {import('express').Response} Res
 * @typedef {import('express').NextFunction} Next
 *
 * @typedef {(req: Req, res: Res, next: Next) => Promise<void>|void} Middleware
 */
